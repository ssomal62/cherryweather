import React from 'react';
import Layout from "../../common/Layout";

const WeatherDetail = () => {
    return (
        <Layout>
            123
        </Layout>
    );
};

export default WeatherDetail;
